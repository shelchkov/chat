export enum InputTypes {
	"EMAIL" = "email",
	"PASSWORD" = "password",
}

export enum ButtonTypes {
	"SUBMIT" = "submit",
	"BUTTON" = "button",
	"RESET" = "reset"
}

export enum RequestMethod {
	"GET" = "GET",
	"POST" = "POST",
}
